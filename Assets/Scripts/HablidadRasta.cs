using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HablidadRasta : MonoBehaviour
{/*
    PlayerController curacionRasta;
    float t;
    // Start is called before the first frame update
    void Start()
    {
        curacionRasta = GetComponent<PlayerController>();
        t = Time.deltaTime;
    }

    // Update is called once per frame
    void Update()
    {
        Vida();
    }

  void Vida()
  {
        if(curacionRasta.health <= 100)
        {
            curacionRasta.health += 10;
            print("f");
        }

        else
        {
            curacionRasta.health = 100;
        }
        
  }*/
}

